<div class="header">
    <div class="mode-switch">
        <div class="active">Reward Stats</div>
        <div>Total Amount</div>
        <div>Total Patrons</div>
    </div>
</div>

<style>
    .header {
        background: var(--panel);
        border-bottom: 1px solid var(--border);
        padding: 12px 20px;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .mode-switch {
        display: flex;
        gap: 6px;
    }

    /* .mode-switch button {
        border: 1px solid var(--border);
        background: #fff;
        padding: 6px 10px;
        border-radius: 6px;
        cursor: pointer;
        font-size: 13px;
    }

    .mode-switch button.active {
        background: var(--accent);
        color: white;
        border-color: var(--accent);
    } */
</style>